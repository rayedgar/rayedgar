# Additional Requirements

- Change product attribute links in "Additional Information" tab to normal text (not clickable).
- Option to switch from tabs into fields that can be placed underneath each other (stacked layout).
- Each field/tab should be customizable in spacing (margin/padding), size (font-size), line-height, and borders.
- Make the standard names of tabs "more information" (Additional Information) and "info" (Description) editable within the elementor widget.
